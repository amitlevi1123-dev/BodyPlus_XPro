version: 1.0
runtime: python3
build:
  commands:
    - pip install --upgrade pip
    - pip install -r requirements.txt
run:
  command: gunicorn -w 2 -k gthread -t 0 -b 0.0.0.0:8080 app.main:app
  network:
    port: 8080
