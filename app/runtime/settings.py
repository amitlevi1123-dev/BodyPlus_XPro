# =============================================================================
# ⚙️ BodyPlus XPro — app/runtime/settings.py
# =============================================================================
# מטרת הקובץ:
# קובץ זה מכיל אך ורק קבועים (Settings) שהיו קודם בתוך main.py.
# אין בו שום לוגיקה — רק ערכים קבועים שהמערכת משתמשת בהם כדי
# להגדיר קצבים, רזולוציות ותדירויות רענון.
#
# למה זה בטוח:
# כל הקבועים זהים לחלוטין למה שהיה קודם ב-main.py.
# הקוד שקורא להם (כולל לולאה, MediaPipe, וידאו וכו’) נשאר זהה לגמרי.
# כלומר — שום דבר במערכת לא משתנה מבחינת התנהגות.
# =============================================================================

# תדירות רענון הדשבורד (עדכון מדדים)
DASHBOARD_HZ = 10
UI_UPDATE_MS = int(1000 / DASHBOARD_HZ)  # כמה מילישניות בין עדכונים

# הגדרה למקסימום רוחב קלט עבור MediaPipe (שיפור ביצועים)
MP_MAX_WIDTH = 720

# כל כמה פריימים לעדכן ידיים (אחד = כל פריים)
HANDS_EVERY_N = 1

# פרק זמן מינימלי בין איטרציות של הלולאה הראשית (מילישניות)
LOOP_INTERVAL_MS = 15

# האם לשקף את התמונה אופקית (למשל במצלמה קדמית)
DEFAULT_MIRROR_X = True
